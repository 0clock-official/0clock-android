<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.OClock" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryVariant">@color/dark_purple</item>
        <item name="colorOnPrimary">@color/main</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/aqua</item>
        <item name="colorSecondaryVariant">@color/dark_aqua</item>
        <item name="colorOnSecondary">@color/main</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@color/main</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground"> @color/main</item>
    </style>
</resources>